[project]
name = "datasettools"
version = "0.1.0"
description = "CLI tools for parsing CoNLL-U files and creating Hugging Face datasets"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "datasets>=3.5.0"
]